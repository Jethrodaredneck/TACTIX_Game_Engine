# TACTIX Ollama Agent

Uses local `qwen2.5-coder:3b` through Ollama. TACTIX must be running. Run `./ollama_agent.sh "task"`, select up to six project-relative files, review the diff, then approve the write. Writes use the existing AI Bridge and its backups.
